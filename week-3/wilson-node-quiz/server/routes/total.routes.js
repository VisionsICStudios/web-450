const express = require('express');
const app = express();
const totalRoute = express.Router();

// Total model
let Total = require('./models/Total');




















module.exports = totalRoute;
